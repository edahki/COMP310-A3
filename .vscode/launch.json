{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug mysh (with test case)",
      "type": "cppdbg",
      "request": "launch",
      "program": "/bin/bash",
      "args": ["${workspaceFolder}/run_debug.sh"],
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "MIMode": "gdb",
      "preLaunchTask": "build-debug"
    }
  ]
}